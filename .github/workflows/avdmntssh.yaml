name: Androidmntssh

on:
  push:
  workflow_dispatch:
    inputs:
      runtime_minutes:
        description: 'How long to keep emulator running (minutes)'
        required: false
        default: '360'

jobs:
  android-emulator:
    runs-on: ubuntu-latest

    env:
      AVD_NAME: android_11
      RUNTIME_MINUTES: ${{ github.event.inputs.runtime_minutes || '360' }}
      VPS_IP: 159.195.6.61
      FRP_SERVER_PORT: 7000
      FRP_VERSION: 0.65.0

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          sudo chmod 666 /dev/kvm 2>/dev/null || true

      - name: Check /mnt Space
        run: |
          echo "=== /mnt Volume ==="
          df -h /mnt
          sudo mkdir -p /mnt/android-sdk-data
          sudo chown -R runner:runner /mnt/android-sdk-data 2>/dev/null || true

      - name: Install x86_64 System Image
        run: |
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH
          sdkmanager "emulator" "platform-tools"
          sdkmanager "system-images;android-30;google_apis;x86_64"

      - name: Create AVD on /mnt
        run: |
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH
          
          sudo mkdir -p /mnt/android-home/.android/avd
          sudo chown -R runner:runner /mnt/android-home
          
          AVD_HOME=/mnt/android-home/.android/avd
          AVD_NAME=${{ env.AVD_NAME }}
          
          echo "AVD Home: $AVD_HOME"
          
          export HOME=/mnt/android-home
          export ANDROID_AVD_HOME=$AVD_HOME
          
          echo "Creating AVD..."
          echo no | avdmanager create avd \
            --force \
            --name "$AVD_NAME" \
            --package "system-images;android-30;google_apis;x86_64" \
            --device "pixel"
          
          sleep 3
          
          AVD_DIR="$AVD_HOME/$AVD_NAME.avd"
          
          if [ ! -d "$AVD_DIR" ]; then
            echo "❌ AVD not created"
            exit 1
          fi
          
          echo "✅ AVD created"
          
          cat >> "$AVD_DIR/config.ini" << 'CONFEND'
          hw.ramSize=12288
          hw.arch=x86_64
          hw.cpu.model=qemu64
          disk.dataPartition.size=40960m
          hw.gpu.mode=auto
          hw.gpu.enabled=yes
          hw.accelerometer=yes
          hw.gps=yes
          hw.battery=yes
          hw.audioInput=yes
          hw.audioOutput=yes
          hw.keyboard=yes
          hw.mainKeys=no
          hw.lcd.density=440
          CONFEND
          
          echo "=== Config ==="
          cat "$AVD_DIR/config.ini" | grep "hw|disk"

      - name: Download FRP Client
        run: |
          mkdir -p /tmp/frp
          cd /tmp/frp
          
          echo "Downloading FRP v${{ env.FRP_VERSION }}..."
          wget -q https://github.com/fatedier/frp/releases/download/v${{ env.FRP_VERSION }}/frp_${{ env.FRP_VERSION }}_linux_amd64.tar.gz
          tar -xzf frp_${{ env.FRP_VERSION }}_linux_amd64.tar.gz
          
          ls -lh frp_${{ env.FRP_VERSION }}_linux_amd64/

      - name: Configure FRP Client (No Auth)
        run: |
          cat > /tmp/frp/frpc.toml << 'FRPCONF'
          serverAddr = "159.195.6.61"
          serverPort = 7000
          
          [[proxies]]
          name = "adb-emulator"
          type = "tcp"
          localIP = "127.0.0.1"
          localPort = 5555
          remotePort = 5555
          FRPCONF
          
          cat /tmp/frp/frpc.toml

      - name: Start Emulator
        run: |
          export HOME=/mnt/android-home
          export ANDROID_AVD_HOME=/mnt/android-home/.android/avd
          export PATH=$ANDROID_HOME/emulator:$ANDROID_HOME/platform-tools:$PATH
          
          AVD_NAME=${{ env.AVD_NAME }}
          
          echo "Starting emulator..."
          nohup emulator -avd "$AVD_NAME" \
            -no-window \
            -no-audio \
            -no-snapshot \
            -accel on \
            -no-metrics \
            > /tmp/emulator.log 2>&1 &
          
          PID=$!
          echo "Emulator PID: $PID"
          sleep 40
          
          adb kill-server 2>/dev/null || true
          sleep 2
          adb start-server
          sleep 5
          
          echo "Waiting for emulator boot..."
          SUCCESS=0
          for i in {1..180}; do
            if adb devices | grep -q "device$"; then
              echo "✅ Emulator booted! ($i sec)"
              SUCCESS=1
              break
            fi
            
            if ! kill -0 $PID 2>/dev/null; then
              echo "❌ Emulator crashed"
              cat /tmp/emulator.log
              exit 1
            fi
            
            if [ $((i % 30)) -eq 0 ]; then
              echo "⏳ Boot progress: $i/180..."
            fi
            
            sleep 1
          done
          
          if [ $SUCCESS -eq 0 ]; then
            echo "❌ Boot timeout"
            cat /tmp/emulator.log
            exit 1
          fi

      - name: Enable ADB TCP
        run: |
          export PATH=$ANDROID_HOME/platform-tools:$PATH
          echo "Enabling ADB over TCP..."
          adb tcpip 5555
          sleep 3
          adb devices

      - name: Start FRP Client
        run: |
          cd /tmp/frp/frp_${{ env.FRP_VERSION }}_linux_amd64
          
          echo "Starting FRP client (no auth)..."
          nohup ./frpc -c /tmp/frp/frpc.toml > /tmp/frpc.log 2>&1 &
          
          FRPC_PID=$!
          echo "FRP Client PID: $FRPC_PID"
          sleep 3
          
          if ! kill -0 $FRPC_PID 2>/dev/null; then
            echo "❌ FRP client failed to start"
            cat /tmp/frpc.log
            exit 1
          fi
          
          echo "✅ FRP client started"
          sleep 2
          cat /tmp/frpc.log | tail -20

      - name: Verify Setup
        run: |
          export PATH=$ANDROID_HOME/platform-tools:$PATH
          sleep 5
          
          echo "========================================="
          echo "✅ SETUP VERIFICATION"
          echo "========================================="
          echo ""
          echo "📱 ADB Devices:"
          adb devices
          echo ""
          echo "🔋 Emulator Memory:"
          adb shell "cat /proc/meminfo | head -2"
          echo ""
          echo "🌐 FRP Status:"
          cat /tmp/frpc.log | tail -10
          echo ""
          echo "🔌 Network Ports:"
          netstat -tuln 2>/dev/null | grep -E "(5555|7000)" || echo "Ports opening..."

      - name: Setup SSH Terminal Access via tmate
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
          connect-timeout-seconds: 120

      - name: Monitor and Keep Running
        run: |
          export HOME=/mnt/android-home
          export ANDROID_AVD_HOME=/mnt/android-home/.android/avd
          export PATH=$ANDROID_HOME/emulator:$ANDROID_HOME/platform-tools:$PATH
          
          RUNTIME_SECONDS=$(((${{ env.RUNTIME_MINUTES }}) * 60))
          HOURS=$(((${{ env.RUNTIME_MINUTES }}) / 60))
          MINS=$(((${{ env.RUNTIME_MINUTES }}) % 60))
          END_TIME=$(($(date +%s) + RUNTIME_SECONDS))
          
          echo "========================================="
          echo "✅ Android 11 Emulator Ready with FRP"
          echo "========================================="
          echo ""
          echo "📊 Configuration:"
          echo "  • OS: Android 11 (API 30)"
          echo "  • Architecture: x86_64"
          echo "  • RAM: 12 GB"
          echo "  • Storage: 40 GB (/mnt)"
          echo "  • Acceleration: KVM"
          echo ""
          echo "🌐 Remote Connection:"
          echo "  • VPS IP: 159.195.6.61"
          echo "  • FRP Port: 7000"
          echo "  • ADB Remote Port: 5555"
          echo ""
          echo "📱 Connect from Your Machine:"
          echo "  adb connect 159.195.6.61:5555"
          echo "  adb devices"
          echo "  adb shell"
          echo "  adb install app.apk"
          echo ""
          echo "🖥️  SSH Terminal:"
          echo "  • Check GitHub Actions logs above"
          echo "  • Look for: ssh [TOKEN]@[host]"
          echo ""
          echo "⏱️  Runtime: $HOURS hours $MINS minutes"
          echo "========================================="
          echo ""
          
          # Auto-restart loop
          while [ $(date +%s) -lt $END_TIME ]; do
            # Check if emulator is alive
            if ! adb devices 2>/dev/null | grep -q "device$"; then
              echo "⚠️  Emulator offline at $(date). Restarting..."
              pkill -f "emulator" || true
              sleep 5
              
              # Restart emulator
              nohup emulator -avd android_11 \
                -no-window \
                -no-audio \
                -no-snapshot \
                -accel on \
                -no-metrics \
                > /tmp/emulator.log 2>&1 &
              
              sleep 40
              
              # Restart ADB
              adb kill-server 2>/dev/null || true
              sleep 2
              adb start-server
              sleep 5
              
              # Wait for boot
              BOOT_SUCCESS=0
              for i in {1..120}; do
                if adb devices | grep -q "device$"; then
                  adb tcpip 5555
                  echo "✅ Emulator restarted at $(date)"
                  BOOT_SUCCESS=1
                  break
                fi
                sleep 1
              done
              
              if [ $BOOT_SUCCESS -eq 0 ]; then
                echo "❌ Failed to restart emulator"
              fi
            fi
            
            sleep 30
          done
          
          echo ""
          echo "========================================="
          echo "⏱️  Runtime completed"
          echo "========================================="
